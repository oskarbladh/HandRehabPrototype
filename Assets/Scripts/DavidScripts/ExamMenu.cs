using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using TMPro;
using System;

public class ExamMenu : MonoBehaviour
{
    public TMP_Text nameText, sizeText, propertyText;

    // Updating text

    /*
     * This is an example of updating the text, 
     * the important thing is to make sure the GameManager 
     * knows which mushroom is being held.
     * 
     public void UpdateMenu(){

     nameText.text = GameManager.instance.GetMushroomName();                  ( Example: "Boletus edulis)
     sizeText.text = GameManager.instance.GetMushroomSize().ToString();       ( Example: "15" )
     propertyText.text = GameManager.instance.GetMushroomProperty();          ( Example: "Miscoloration - Eaten by an animal - Moldy" )
    */
}
